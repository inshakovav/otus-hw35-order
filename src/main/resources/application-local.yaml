server:
  port: 8000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5440/db_otus_35
    username: postgres
    password: postgres
#  flyway:
#    enabled: true
#    schemas: scheme

  kafka:
    bootstrap-servers: localhost:29092